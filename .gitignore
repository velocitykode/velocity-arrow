# Internal docs (synced to ~/vdocs via vdocs-sync, not shipped)
/docs/*.md
/docs/packages/
/docs/implementation/

# built binary
/arrow

# sqlite sidecars - opening the baked snapshot with the sqlite3 CLI leaves
# these behind, and `git add -A` would commit them alongside the .db
*.db-shm
*.db-wal
